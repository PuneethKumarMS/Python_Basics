numbers = [1, 2, 4, 11, -2, 0, 85]

for i in numbers:
    print(i)
    
    if i == 0:
        break # here for loop is terminated forcefully

else:
    print("Successfully Loop Executed") # this block would be executed only after the completion of for_loop
